model: hgt
hidden_channels: 128
out_channels: 1
n_heads: 4
n_layers: 2
dropout: 0.2
node_types:
  - transaction
  - card
  - address
edge_types:
  - [transaction, uses, card]
  - [transaction, ships_to, address]
lr: 1.0e-3
max_epochs: 100
batch_size: 256
data_dir: data/graph
